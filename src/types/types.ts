export type characterDataType = {
  theme: string;
  name: string;
  species: string;
  gender: string;
  age: string;
  role: string;
  appearance: string;
  history: string;
};

export type locationDataType = {
  theme: string;
  name: string;
  appearance: string;
  history: string;
};

export type Link = {
  href: string;
  text: string;
};
